<div>
  <div class="space-x-4 border rounded border-gray-950 shadow-2xl">
    <div class="wrap">
      <h2 class="m-8 text-4xl font-medium">
        Аналитика бота 
      </h2>
      
      <h2 class="m-8 text-2xl">
        Всего пользователей в боте: 
      </h2>

      <% Category.all.each_with_index do |category, index| %>
        <p class="ml-0.5 text-2xl"><%= index+1%>. <%= category.name%>: <%= category.user.size%> </p>
      <% end %>
    </div>
  </div>  
</div>
