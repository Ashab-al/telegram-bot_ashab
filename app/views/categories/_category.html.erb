<div id="<%= dom_id category %>">

  <% if action_name != "show" %>
    <p class=""><%= category.name %></p>
    <%= link_to "Редактировать", category_path(category), class: "rounded-lg py-3 ml-2 px-5 bg-gray-100 inline-block font-medium" %>
    <hr class="mt-6">
  <% end %>

</div>
